package solid.ConceptAndCoding.LiskovSubstitutionPrinciple;

public class Main {
    // L - Liskov Sustituion Principle
    // NOTE: If Class B is subtype of Class A, then we should be able to
    // replace an object of A with object of Class B, without breaking the
    // behaviour of the program.

    // Crux - Subclass should extend the capability of parent class not narrow it down.

}
